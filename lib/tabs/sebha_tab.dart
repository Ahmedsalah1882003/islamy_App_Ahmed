import 'package:flutter/material.dart';

class SebhaTabe extends StatelessWidget {
  const SebhaTabe({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
